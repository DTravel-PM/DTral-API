{
  "routes": [
    {
      "method": "GET",
      "path": "/pending-lists",
      "handler": "pending-list.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pending-lists/count",
      "handler": "pending-list.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pending-lists/:id",
      "handler": "pending-list.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pending-lists",
      "handler": "pending-list.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pending-lists/:id",
      "handler": "pending-list.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pending-lists/:id",
      "handler": "pending-list.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
